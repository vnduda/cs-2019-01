/**
 * Função para encontrar a menor temperatura em um vetor.
 * @param vetor de valores recebidos
 * @returns menor elemento do vetor
 */
function menorTemperatura (array) {
    
    let menorElemento = Math.min(array);
    
    return menorElemento;
}